source ~/.pentadactyl/bepo.penta

set defsearch=seeks-search
set newtab=all
set titlestring="RFC 2616 bot"
set hlfind
set findcase=smart
set! ui.key.generalAccessKey=0

map , <Leader>

map <Leader>t -js io.run("task", ["add", "pro:alire "+buffer.URL], false); dactyl.echomsg("Added task for " + buffer.URL);
