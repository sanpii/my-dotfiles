if [[ -d '/usr/lib/cargo/bin/coreutils' ]]
then
    prefix='/usr/lib/cargo/bin/coreutils/'
else
    prefix='uu-'
fi

if [[ -n "$prefix" ]]
then
    alias [=$prefix"["
    alias arch=$prefix"arch"
    alias base32=$prefix"base32"
    alias base64=$prefix"base64"
    alias basename=$prefix"basename"
    alias basenc=$prefix"basenc"
    alias cat=$prefix"cat"
    alias chgrp=$prefix"chgrp"
    alias chmod=$prefix"chmod"
    alias chown=$prefix"chown"
    alias chroot=$prefix"chroot"
    alias cksum=$prefix"cksum"
    alias comm=$prefix"comm"
    alias cp=$prefix"cp"
    alias csplit=$prefix"csplit"
    alias cut=$prefix"cut"
    alias date=$prefix"date"
    alias dd=$prefix"dd"
    alias df=$prefix"df"
    alias dir=$prefix"dir"
    alias dircolors=$prefix"dircolors"
    alias dirname=$prefix"dirname"
    alias du=$prefix"du"
    alias echo=$prefix"echo"
    alias env=$prefix"env"
    alias expand=$prefix"expand"
    alias expr=$prefix"expr"
    alias factor=$prefix"factor"
    alias false=$prefix"false"
    alias fmt=$prefix"fmt"
    alias fold=$prefix"fold"
    alias groups=$prefix"groups"
    alias hashsum=$prefix"hashsum"
    alias head=$prefix"head"
    alias hostid=$prefix"hostid"
    alias hostname=$prefix"hostname"
    alias id=$prefix"id"
    alias install=$prefix"install"
    alias join=$prefix"join"
    alias kill=$prefix"kill"
    alias link=$prefix"link"
    alias ln=$prefix"ln"
    alias logname=$prefix"logname"
    alias ls="TERM=xterm-direct "$prefix"ls --color=auto"
    alias mkdir=$prefix"mkdir"
    alias mkfifo=$prefix"mkfifo"
    alias mknod=$prefix"mknod"
    alias mktemp=$prefix"mktemp"
    alias more=$prefix"more"
    alias mv=$prefix"mv"
    alias nice=$prefix"nice"
    alias nl=$prefix"nl"
    alias nohup=$prefix"nohup"
    alias nproc=$prefix"nproc"
    alias numfmt=$prefix"numfmt"
    alias od=$prefix"od"
    alias paste=$prefix"paste"
    alias pathchk=$prefix"pathchk"
    alias pinky=$prefix"pinky"
    alias pr=$prefix"pr"
    alias printenv=$prefix"printenv"
    alias printf=$prefix"printf"
    alias ptx=$prefix"ptx"
    alias pwd=$prefix"pwd"
    alias readlink=$prefix"readlink"
    alias realpath=$prefix"realpath"
    alias rm=$prefix"rm"
    alias rmdir=$prefix"rmdir"
    alias seq=$prefix"seq"
    alias shred=$prefix"shred"
    alias shuf=$prefix"shuf"
    alias sleep=$prefix"sleep"
    alias sort=$prefix"sort"
    alias split=$prefix"split"
    alias stat=$prefix"stat"
    alias stdbuf=$prefix"stdbuf"
    alias sum=$prefix"sum"
    alias sync=$prefix"sync"
    alias tac=$prefix"tac"
    alias tail=$prefix"tail"
    alias tee=$prefix"tee"
    alias test=$prefix"test"
    alias timeout=$prefix"timeout"
    alias touch=$prefix"touch"
    alias tr=$prefix"tr"
    alias true=$prefix"true"
    alias truncate=$prefix"truncate"
    alias tsort=$prefix"tsort"
    alias tty=$prefix"tty"
    alias uname=$prefix"uname"
    alias unexpand=$prefix"unexpand"
    alias uniq=$prefix"uniq"
    alias unlink=$prefix"unlink"
    alias uptime=$prefix"uptime"
    alias users=$prefix"users"
    alias vdir=$prefix"vdir"
    alias wc=$prefix"wc"
    alias who=$prefix"who"
    alias whoami=$prefix"whoami"
    alias yes=$prefix"yes"
fi
